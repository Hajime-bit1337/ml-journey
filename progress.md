# Мой контекст

- **Времени в неделю:** сейчас каникулы, 35-45 часов (5-7 ч/день).
  После каникул упадёт до ~10-15 ч/неделю. Тяжёлые блоки грузим сейчас.
- **Цель:** быть готовым подаваться на junior ML/DS через 4 месяца.
  Рассылку резюме начинаю с 10-й недели, не позже.
  Понимаю, что оффер может прийти позже — это нормально.
- **Python:** уверенный
- **Математика:** 1 курс вуза (линал и матан в общих чертах есть, статистики нет)
- **ОС:** Linux (Ubuntu 24.04), zsh. `python` не существует, только `python3`
  (внутри venv работает `python`).
- **Level 0:** терминал частично, git почти не знаю, venv не знал —
  закрываем на первой неделе, не растягиваем.

---

# СОСТОЯНИЕ

**Дата:** 23.08.2026
**Старт обучения:** 11.08.2026 (неделя 1 из 16)
**Текущий этап:** **Level 0 и Level 1 закрыты.** Репозиторий с README и проектом
EDA по Titanic на GitHub. Следующий — Level 2: статистика Карпова (обе части),
линал и матан под ML с реализацией руками на numpy.
План отводил на Level 1 три недели, ушло пять дней. Опережение — около двух недель.
Использовать его на статистику: её нет совсем, а она нужна везде дальше.

**Закрыто:**
- Python — уверенно (до начала роадмапа)
- Базовая математика 1 курса вуза
- venv: разобран до механизма (11.08.2026, уверенность 4/5). На входе в сессию
  провалил все три вопроса, вывел модель сам из наблюдений: в `.venv/` нет Python,
  только symlink на `/usr/bin/python3`; изолированы библиотеки через
  `include-system-site-packages = false`; `activate` дописывает `.venv/bin`
  в начало PATH; работает только через `source`, потому что дочерний процесс
  не может изменить окружение родителя.

- Git, базовый цикл (11.08.2026, уверенность 4/5): три зоны
  (рабочий каталог → индекс → коммит), `status`/`diff`/`diff --staged`/`add`/`commit`/`log`,
  отмена через `restore` и `restore --staged`, ветки как указатели на коммит,
  `switch -c`/`merge` (fast-forward)/`branch -d`, `.gitignore` и `git check-ignore -v`.
  Наступил на грабли: `restore` берёт версию **из индекса**, а не из последнего коммита.
- SSH-ключ и GitHub (11.08.2026): ключ ed25519 создан и добавлен в аккаунт,
  `ssh -T git@github.com` проходит, репозиторий
  https://github.com/Hajime-bit1337/ml-journey запушен, `main` привязана к `origin/main`.

- Kaggle Learn «Pandas» — все 6 уроков (12.08.2026, уверенность 3/5).
  Конспект с работающим кодом: `notebooks/Kaggle5-6.ipynb`.
  Разобрано на своей шкуре: методы возвращают копию, а не меняют объект
  (`astype` не поменял `int64`); один пропуск переводит целую колонку в float;
  `join` размножает строки, если ключ не уникален **в правой** таблице;
  `NaN` после join = ключи не сошлись, а не «данных нет».
  Слабое: `merge` не трогали (урок Kaggle его опускает) — добрать отдельно.

- NumPy — база закрыта (12.08.2026, уверенность 3/5).
  Практика: `notebooks/02_numpy_basics.ipynb` — нормализация признаков
  циклами и векторно, `np.allclose` → True, средние ≈0, std = 1.
  Разобрано:
- Зачем numpy: замерил сам на 5 млн чисел — цикл 0.36 с, numpy `int64` 0.012 с,
  `object` 3.73 с (медленнее обычного цикла в 10 раз). Память на 1 млн чисел:
  8 МБ у numpy против 36 МБ у списка.
- `int64` переполнился молча и выдал мусор; `float64` влезает по величине, но
  теряет младшие разряды (~15-16 значащих цифр). Вывод: dtype — инженерный
  выбор между точностью, памятью и скоростью; отсюда `float32` в DL.
- Скорость numpy = фиксированный размер ячейки + непрерывная память + цикл в C.
  Цена — переполнение без ошибки и один тип на весь массив.
- `shape`/`ndim`/`size`, `reshape` (в т.ч. `-1`), reshape не копирует данные —
  запись через `b` видна в `a`.
- Оси: **та ось, которую называешь в `axis`, из результата исчезает**.
  Приём перед выбором axis: сказать, сколько чисел должно быть в ответе.
  Сначала перепутал axis на задаче «средний расход клиента», после проверки
  на числах переформулировал верно сам.

- Broadcasting: формы выравниваются **справа налево**, пара совместима, если
  числа равны или одно равно 1; недостающие оси слева считаются единицами.
  `(3,4)+(4,)` работает, `(3,4)+(3,)` падает, `(3,4)+(3,1)` работает.
  `v[:, np.newaxis]` → столбец, `v[np.newaxis, :]` → строка; ось встаёт туда,
  где написана. Главная опасность: `(3,4)+(3,1,1)` не падает, а молча
  выдаёт `(3,3,4)` — 36 чисел вместо 12. Правило: после каждой операции
  с разными формами сверять `.shape` с ожиданием.
- Float: среднее вышло `-7.3e-18`, а не 0. Вещественные не сравнивают через
  `==`, только `np.allclose`/`np.isclose`.
- Разовый замер быстрого кода — шум. Нужен `timeit` / `%timeit`.
- Домашка (min-max нормализация) сдана с двух попыток: min = 0, max = 1 выведены,
  случай постоянного столбца воспроизведён (`RuntimeWarning` + `nan`, не исключение).
  Хвост в тексте: назвал предупреждение ошибкой; объяснение, почему min-max от `X`
  и от z-нормализованного `X2` совпадают, дал я — `(x−a)/b` сокращается.

Не закрыто по NumPy, добрать позже:
- булевы маски и fancy indexing в numpy, `np.where`
- матричные операции: `@`, `dot`, транспонирование (понадобятся на Level 2)
- `axis` у трёхмерных массивов

- SQL — база пройдена за один день (13.08.2026, уверенность 3/5).
  Своя учебная база: `sql/create_db.py` создаёт `shop.db` (8 клиентов, 40 заказов,
  один клиент без заказов, один заказ-сирота, один `NULL` в городе).
  Запросы и разборы: `notebooks/03_sql_basics.ipynb`.
  Пройдено: SELECT/WHERE/ORDER BY/LIMIT/DISTINCT; GROUP BY, HAVING;
  JOIN (INNER, LEFT); оконные функции (`OVER`, `PARTITION BY`, `ROW_NUMBER`,
  `RANK`, `DENSE_RANK`, `LAG`), CTE через `WITH`.
  Разобрано на своей шкуре:
  - `NULL` — трёхзначная логика. `city = NULL` даёт 0 строк, `city != 'Париж'`
    выбрасывает строку с `NULL` из обеих выборок. `IS NULL` / `IS NOT NULL`.
    `COUNT(*)` против `COUNT(колонка)` — способ найти пропуски.
    `NULL` не равен себе при сравнении, но группируется в одну корзину.
  - Порядок выполнения: FROM → WHERE → GROUP BY → HAVING → окна → SELECT →
    ORDER BY → LIMIT. Отсюда: в `WHERE` нет агрегатов, в `WHERE`/`HAVING` нет
    оконных функций (нужен CTE), псевдоним из `SELECT` виден только в `ORDER BY`.
  - JOIN меняет «единицу строки»: после соединения `COUNT(*)` считает заказы,
    а не клиентов. Клиентов из Парижа стало 14 вместо 2. Лечится `COUNT(DISTINCT)`
    или отказом от лишнего join.
  - `GROUP BY c.name` вместо `c.id` склеил двух разных Никит и потерял клиента.
    Группировать по ключу, а не по названию.
  - `ORDER BY` внутри `OVER` задаёт смысл соседства, а не порядок вывода:
    в выводе `NaN` от `LAG` оказался в середине таблицы, а не сверху.
  Молчаливые ошибки, пойманные по дороге: `np.int64` записался в базу как BLOB
  (сломал бы JOIN без единой ошибки); опечатка `PRIMRY KEY` отключила первичный
  ключ, и SQLite приняла её как имя типа; даты с точками ломают функции дат.

- Визуализация закрыта (14-15.08.2026, уверенность 3/5).
  Практика: `notebooks/04_viz_basics.ipynb`.

- **Проект Level 1 сдан (15.08.2026): `notebooks/05_titanic_eda.ipynb`.**
  891 запись, 9 выводов с числами, раздел ограничений, ноутбук прогоняется
  начисто (execution_count 1…52 без пропусков). README в корне репозитория.
  Ключевое, что освоено на проекте:
  - **утечка целевой переменной**: `alive` оказалась дословной копией `survived`,
    проверено `crosstab`. Симптом утечки — подозрительно высокая метрика;
  - **confounding**: связь «пропуск палубы → смерть» полностью объясняется
    классом (палуба записана у 81% первого класса и 2% третьего). То же с
    возрастом и портом;
  - **приём проверки**: чтобы понять, свой ли эффект у фактора A, смотреть A
    внутри каждого уровня B. Пол и класс так проверены — эффекты независимы,
    разрыв сохраняется в обе стороны;
  - заполнение пропусков строкой ломает dtype колонки (`float64` → `object`),
    и после этого не считается ни медиана, ни фильтр по возрасту;
  - флаг пропуска (`age_missing`) заводится **до** заполнения, а анализ ведётся
    на неискажённой части;
  - нулевая корреляция ≠ отсутствие связи: `age`↔`survived` даёт −0.05, хотя
    дети выживали вдвое чаще, — Пирсон видит только линейное;
  - размер группы смотреть до сравнения долей: «66% выживших во втором классе
    Квинстауна» — это двое из трёх.

**Ранее в работе (14.08.2026), пройдено:**
- Matplotlib: Figure против Axes, `plt.subplots`, явный стиль через `ax` вместо
  глобального `plt`, `figsize` в дюймах против `dpi` в точках, `savefig`,
  сетка графиков как numpy-массив `axes[1, 1]`.
- Seaborn: работает с DataFrame по именам колонок, сам агрегирует, возвращает
  те же `Axes`. Осевого уровня (`histplot`, `boxplot`, `scatterplot`, `heatmap`)
  против фигурного (`pairplot`, `relplot`) — вторые не принимают `ax`.
- Разобрано на своей базе `shop.db`: `bins` меняет выводы, а не данные; boxplot
  показывает медиану и квартили, медиана устойчива к выбросам; усы у `barplot` —
  доверительный интервал 95%, а не разброс; `estimator='sum'` против среднего
  даёт разные ответы на вопрос «какой город лучший».
- Корреляция по колонке-идентификатору (`customer_id`) — бессмысленное число.
  Тип `int64` не делает колонку количественной. Прямая подводка к Level 3.

- Pandas — беглость подтянута (18.08.2026, уверенность 4/5).
  Практика: `notebooks/06_pandas_drill.ipynb`, ~50 задач с чистой ячейки.
  Причина сессии: сам сказал «не вдуплял, что вводить, как будто и не проходил
  курсы». Подсчёт по ноутбукам подтвердил: `groupby` 25 раз, `mean` 22,
  а `.loc[` и `.iloc[` по одному разу за всё время, `merge` / `pivot_table` /
  `apply` / `isin` / `cut` — ноль. Один инструмент на все задачи.
  Правило сессии: я не даю синтаксис, только направление; ищет в документации сам.

  Замер: **вход** — 10 однострочников за 40 минут, искал `loc`, задачи 8-10.
  **Выход** — 13 задач сложнее входных, 12 верных, без единой подсказки.

  Освоено и написано руками: `loc`/`iloc` с обеими осями, срезы по меткам против
  позиций, `isin`, `between`, `query`, `value_counts(normalize)`,
  `agg` именованная и списком, `as_index=False`, `transform` с `rank`,
  `pd.cut` с `labels`, `groupby().filter()`, `idxmax` + `loc` для строки целиком,
  `merge` со всеми `how`, `validate='m:1'`, `indicator=True`,
  `pivot_table` с `margins`, `crosstab` с `normalize`.

  Разобрано по ходу:
  - `df.loc[100]` даёт Series, `df.loc[[100]]` — DataFrame. Одиночная метка
    понижает размерность, список сохраняет.
  - **`NaN != 'x'` в pandas даёт `True`**, в SQL `NULL != 'x'` выбрасывает строку.
    Одна операция, противоположное поведение. Проверено: 247 строк против 245.
  - `sum` по пустой группе: pandas даёт 0, SQL даёт `NULL`.
  - суффиксы `_x`/`_y` там, где не ждёшь, — признак, что соединяешь не то.
  - решение, работающее только потому, что нужная строка одна
    (`sort_values().iloc[-1]`), — не решение.
  - в pandas 3.0 включён Copy-on-Write: `SettingWithCopyWarning` больше нет,
    `.copy()` для корректности не нужен. Почти всё в интернете написано про 2.x —
    сверять `pd.__version__`, когда поведение расходится с описанным.

- Статистика, описательные меры (18.08.2026, уверенность 3/5).
  Практика: `notebooks/07_stats_basics.ipynb`. Всё считал руками на numpy,
  готовые функции только для сверки.
  - Меры центра: среднее, медиана, мода. Медиане важен **порядок**, а не величина,
    поэтому она устойчива к выбросам: один директор с 500 сдвинул среднее
    с 35 до 101, медиану — никуда. На `fare` среднее 32 против медианы 14.
    Мода осмысленна для 3 портов и бессмысленна для 248 разных цен.
  - Вывел дисперсию по шагам: сумма отклонений от среднего всегда ноль
    (следует из определения среднего), поэтому знак убирают квадратом.
    Квадрат реагирует на выброс втрое сильнее модуля (×12.25 против ×4),
    корень возвращает исходные единицы.
  - **`ddof`**: `np.std` делит на `n`, `pd.Series.std` — на `n−1`. Нашёл сам,
    методом гипотез: предсказал совпадение, не сошлось, проверил своей функцией,
    отверг версию про погрешность, попал со второй.
    Симуляция подтвердила смещение: при выборках по 5 `ddof=0` даёт 180
    вместо истинных 225, `ddof=1` — 225.07. При n=30 разрыв почти исчезает.
  - Поправка `n−1` делает несмещённой **дисперсию**, а не std: корень нелинеен,
    поэтому `std` с `ddof=1` всё равно чуть занижен (14.89 против 15.01).
  - `np.append` в цикле копирует массив целиком каждый раз: 20000 итераций —
    0.152 с против 0.002 с у списка с превращением в массив в конце.

- Статистика, распределения и ЦПТ (21.08.2026, уверенность 3/5).
  Практика: `notebooks/08_distributions.ipynb`.
  - Нормальное распределение: μ двигает, σ растягивает. Правило трёх сигм
    получил симуляцией: 67.7 / 94.8 / 99.65 на 10000 значениях.
  - **Признак «модель не подходит»: подставить μ−3σ и спросить, бывает ли такое
    значение.** Для `fare` границы вышли −17, −67, −117 фунтов; отрицательных цен
    не бывает. Тот же симптом позже вылез в доверительном интервале для доли.
  - Асимметрия: `fare` +4.79, `age` +0.06. Признак — **медиана меньше среднего
    → длинный хвост справа**. Вывел сам, сначала перепутал сторону, поймал себя
    по гистограмме и переписал вывод.
  - `log1p` сжал асимметрию `fare` с 4.79 до 0.39 — в 12 раз, при том что на глаз
    «почти не изменилось». Урок: `.skew()` вместо глаза.
  - **ЦПТ выведена симуляцией на своих данных.** Брал 10000 выборок из `fare`
    (асимметрия 4.79) при n = 2, 5, 30, 100, 400: среднее средних всегда 32.2,
    асимметрия падает 3.38 → 2.03 → 0.82 → 0.50 → 0.25, разброс сжимается.
    Отдельно отмечено: при n=30 асимметрия ещё 0.82, то есть школьное правило
    «n ≥ 30 достаточно» на тяжёлых хвостах не работает.
  - **Вывел `SE = σ/√n` сам из таблицы отношений** и придумал собственную проверку:
    взял n = 3600 (в 9 раз больше 400), предсказал уменьшение разброса втрое,
    подтвердил. Сформулировал как «уменьшить разброс в x раз → увеличить n в x²».
  - SE для доли: `√(p(1−p)/n)`. На группе «старше 64» (1 из 11) SE = 0.087
    при p = 0.091 — ошибка равна самой оценке, интервал от 0 до 26%.
    **Правило: сравнивать SE с величиной оценки, а не смотреть на модуль.**

- Доверительные интервалы (23.08.2026, уверенность 3/5).
  Практика: `notebooks/09_confidence_intervals.ipynb`.
  - Собрал интервал из трёх уже известных кусков: ЦПТ → распределение оценок
    нормально, `SE` → его разброс, правило двух сигм → 95%. Множитель 1.96.
  - **Симуляция покрытия:** 100 интервалов по выборкам из известной совокупности,
    накрыло 95. Случаен **интервал**, а не истинное значение — оно константа.
  - Применил SE к результату собственной симуляции: 91 против 95 при 100 повторах
    укладывается в её собственную ошибку ±2.2 п.п.
  - Три фактора ширины: размер выборки (как `1/√n`), разброс данных,
    уровень доверия. Точность и уверенность обменивают друг на друга.
  - **Где ломается:** `p ± 1.96·SE` — нормальная модель, определённая на всей
    прямой, а доля живёт в `[0,1]`. При n=3 и p=0.667 верхняя граница вышла 1.211.
    Правило применимости: не меньше 5 успехов и 5 неудач в группе.
  - Отношение ширины к оценке: старики 3.8, Квинстаун 1.6, мужчины 3 класса 0.54.
    Первым двум оценкам верить нельзя, третьей можно.
  - Пойманные баги: `g.append(SE)` дал список из 200 элементов вместо 100,
    и `if g[i]:` молча красил отрезки в зелёный, потому что **любое ненулевое
    число в Python истинно**. Ученик заметил расхождение сам — по картинке,
    где зелёные отрезки не касались линии.
  - `ax.vlines?` не работает, если объект ещё не создан; у класса спрашивать
    можно всегда: `plt.Axes.axhline?`.

**В работе:** ничего.

**Не начато:**
- Level 2: статистика (Карпов, обе части), линал и матан под ML

**Провалено на проверке / вернуться:**
- Терминал, пути: спутал текущий каталог, на «нет такого файла» полез в
  `sudo find /` вместо `pwd`. Отработать: относительные vs абсолютные пути,
  `pwd` как первая реакция на «файл не найден», `sudo` только когда правда нужен.
- Привычка отвечать гипотезой вместо запуска команды. Проверять, а не угадывать.
- **Главное и не исправлено — недостаточная сверка с письменным источником.**
  Проявляется двояко и держится третью сессию подряд:
  1. Текст расходится с выводом ячейки над ним. 15.08 — шесть раз
     (`alive`, причинность палубы, «у детей шанс ниже», пропуски по классам,
     мёртвая проверка по `deck`, разрыв цен).
  2. Код расходится с условием задачи: теряется вторая половина требования.
     16.08 — пять раз («колонки от sex до fare», «внутри своего класса»,
     «и количество», «внутри каждой строки», «целыми строками»).
     Каждый раз дописывал за минуту после указания — то есть умеет, но не сверяет.
  Ритуал: выписать требования списком → написать код → пройти по списку
  и отметить каждое. Написал фразу с числом → найти это число в выводе глазами.
- Повторил ошибку двухдневной давности: `groupby('name')` вместо `groupby('id')`
  на базе с двумя Никитами — 7 строк вместо 8, клиент молча исчез.
  Предсказал 7, получил 7, дальше не смотрел. Сверять надо с реальностью
  (клиентов восемь), а не со своим ожиданием.
- pandas: методы возвращают новый объект, а не меняют исходный
  (`astype`, `rename`, `fillna`, `sort_values`, `drop`). Не усвоено.

**Активный проект:** нет

**Домашка на следующий раз:**
1. Доделать `09_confidence_intervals.ipynb`: вердикт по правилу пяти, отношение
   ширины к оценке, список трёх факторов, задачи 1-3 прогнать кодом, а не по памяти.
   Плюс `Restart & Run All` — там устаревший вывод `len` и текст «91 случай»
   при выводе 0.95.
2. Таблица доверительных интервалов по Titanic: доля выживших по классам и по полу,
   колонки — оценка, SE, границы, размер группы. Прочитать: где интервалы
   не пересекаются, где пересекаются.
3. Связать с усами `sns.barplot`: убедиться, что `errorbar=('ci', 95)` даёт
   примерно то же, что посчитано руками. Это закрывает вопрос, с которого
   началась вся тема статистики.
4. Курс Карпова, часть первая — продолжать.

**Старая домашка:**
1. Задачи 2 и 3 из блока про стандартную ошибку: две модели (0.87 против 0.89
   на тесте из 200) и сравнение узкой группы с широкой на Titanic.
2. Поправить формулировку про стариков в `05_titanic_eda.ipynb`: не «шанс 9%»,
   а «1 из 11, интервал от 0 до 26%, размер группы не позволяет оценить долю».
3. Добить долги по `08_distributions.ipynb`: ячейка со `.skew()`, убрать фразу
   «график не сильно отличается», убрать неверную первую строку про логарифм.
4. Курс Карпова на Stepik, часть первая.

**Домашка сдана (12.08.2026), обе части.** Понадобилось 5 кругов проверки:
первые три правил форму (ячейки, markdown, выполнение), последние два —
содержание. К пятому кругу разбирал причины ошибок сам и верно.
Хвост на будущее: в `01_pandas_basics.ipynb` стиль — CamelCase вместо snake_case,
лишние `print()`, markdown описывает «что я сделал» вместо «что делает метод».

**Моя ошибка в разборе (для честности счёта):** сказал, что он перепутал
`merge`/`join`. Урок Kaggle действительно пишет, что большинство возможностей
`merge()` проще получить через `join()`. Конспект был верен, поправка снята.

**Домашка по SQL сдана (13.08.2026), с трёх попыток.** `sql/01_basics.sql`:
12 запросов (10 моих тем + 2 своих), все выполняются без ошибок, ответы приложены.
Предсказания числа строк совпали везде, включая `7*2+1` с учётом сироты.
Круги ушли на содержание, не на синтаксис:
- сначала «6 людей с самыми бедными заказами» вернуло первым `999` — не человека:
  join был от `orders`, группировка по колонке с мусором. Направление `LEFT JOIN`
  выбирается из формулировки вопроса;
- после исправления `COUNT(*)` дал 1 заказ клиенту без заказов — строка от
  `LEFT JOIN` есть, заказа нет. Лечится `COUNT(o.id)`;
- правку запушил не прогнав файл: переименовал псевдоним `mounth`→`month` только
  в `SELECT`, запрос упал. **Изменил — перезапусти**, это повторяется третий раз.
Добрано по ходу: рамка окна (`ORDER BY` внутри `OVER` меняет рамку по умолчанию
с «весь раздел» на «от начала до текущей строки» — отсюда накопительный итог);
`LEFT JOIN` + `WHERE правая IS NOT NULL` ≡ `INNER JOIN`;
псевдоним из `SELECT` в `HAVING` — поблажка SQLite, PostgreSQL отвергнет.

**Проверить в начале следующей сессии:** разница `WHERE` и `HAVING` через порядок
выполнения; чем оконная функция отличается от `GROUP BY` (ответ — про число строк);
почему `WHERE rn <= 3` не работает и что вместо; что вернёт `SELECT * FROM t
WHERE col != 'x'`, если в `col` есть `NULL`.

**Темы на интервальное повторение:**
- venv и PATH (спросить снова ~18.08.2026)
- `source` vs запуск скрипта, дочерний процесс (спросить снова ~18.08.2026)
- pandas возвращает копию, а не меняет объект (спросить снова ~19.08.2026)
- ловушки `join`: неуникальный ключ справа → дубли строк; `NaN` → ключи
  не сошлись (спросить снова ~19.08.2026)
- broadcasting: выравнивание форм справа налево (спросить снова ~19.08.2026)
- `axis` схлопывает названную ось (спросить снова ~19.08.2026)
- `NULL` и трёхзначная логика в SQL (спросить снова ~20.08.2026)
- порядок выполнения запроса, `WHERE` vs `HAVING` (спросить снова ~20.08.2026)
- окно не схлопывает строки; фильтрация по окну только через CTE
  (спросить снова ~20.08.2026)
- JOIN меняет единицу строки, `COUNT(DISTINCT)` (спросить снова ~21.08.2026)
- `COUNT(*)` против `COUNT(колонка)` после `LEFT JOIN` (спросить снова ~21.08.2026)
- рамка окна: почему `ORDER BY` в `OVER` даёт накопительный итог
  (спросить снова ~22.08.2026)
- утечка целевой переменной: определение и симптом (спросить снова ~22.08.2026)
- confounding и приём «смотреть A внутри уровней B» (спросить снова ~23.08.2026)
- нулевая корреляция ≠ отсутствие связи (спросить снова ~24.08.2026)
- `agg` схлопывает, `transform` сохраняет длину — как `GROUP BY` против окна
  (спросить снова ~24.08.2026)
- `NaN != 'x'` в pandas → True, в SQL → строка выпадает (спросить снова ~23.08.2026)
- группировать по ключу, а не по названию — повторял дважды, оба раза ошибся
  (спросить снова ~19.08.2026, потом ~26.08.2026)
- `merge`: `how`, `validate`, `indicator` (спросить снова ~25.08.2026)
- почему сумма отклонений от среднего равна нулю (спросить снова ~25.08.2026)
- `ddof`: `np.std` делит на n, `pd.std` на n−1; зачем поправка
  (спросить снова ~26.08.2026)
- медиана устойчива к выбросам, потому что зависит от порядка
  (спросить снова ~27.08.2026)
- ЦПТ: распределение средних нормализуется, `SE = σ/√n` (спросить снова ~28.08.2026)
- признак асимметрии: медиана меньше среднего → хвост справа (спросить ~28.08.2026)
- подставить μ−3σ и проверить, бывает ли такое значение (спросить ~29.08.2026)
- SE сравнивают с величиной оценки, а не смотрят на модуль (спросить ~29.08.2026)
- что именно случайно в доверительном интервале (спросить ~30.08.2026)
- три фактора ширины интервала (спросить ~30.08.2026)
- правило «5 успехов и 5 неудач», границы за пределами [0,1] (спросить ~31.08.2026)
- в Python истинно любое ненулевое число (спросить ~26.08.2026)

---

# Журнал сессий

<!-- Каждая сессия — одна строка: дата, что делали, вердикт -->

| Дата | Тема | Вердикт |
|---|---|---|
| 11.08.2026 | Настройка окружения, venv, git init | — |
| 11.08.2026 | venv/PATH/source; git: три зоны, restore, ветки, merge; SSH + первый push | Зачёт. Venv провален на входе, выведен заново самостоятельно. Git отработан на живом репозитории. Слабое место — отвечает гипотезой вместо проверки командой. |
| 12.08.2026 | Разбор домашки: ноутбук по loc/iloc/groupby, конспект Kaggle 5-6 | Зачёт с пятой попытки. Форму правит быстро, содержание — медленно. Сдвиг за сессию: к концу сам находил причины ошибок по выводу ячеек, без подсказки. Главная привычка (читать вывод, а не только запускать) начала появляться. |
| 12.08.2026 | NumPy: dtype и переполнение, shape и оси, broadcasting, векторизация | Зачёт. Проверку по Pandas на входе провалил (2 из 4), после дриллинга пересдал. Заметный сдвиг: сам переписал свой неверный ответ про reshape, поигравшись с командой, и сам заметил расхождение сумм в замере. Практика сдана с двух попыток вместо пяти. Слабое: путает механизм с метафорой — объясняет «своими словами» там, где нужна точная формулировка. |
| 13.08.2026 | SQL с нуля: своя база, SELECT/WHERE, NULL, GROUP BY/HAVING, JOIN, оконные функции | Зачёт. Весь список SQL из роадмапа за один день (~10 часов). Сильный сдвиг: сам заметил BLOB вместо чисел в выводе, сам поймал сироту 999 при предсказании, сам починил путь к базе по симптому «файл не найден». Слабое: дважды приписал верно понятый механизм не тому запросу (WHERE/HAVING, LEFT JOIN) — понимает, но не сверяется с текстом перед ответом. Практическое задание перенесено в домашку: сессия затянулась. |
| 14.08.2026 | Matplotlib (Figure/Axes), Seaborn, начало EDA по Titanic | Прервано на середине из-за усталости — сам попросил перерыв после двух десятичасовых дней. Решение верное. Пройдено: устройство matplotlib, типы графиков seaborn, доверительный интервал у barplot, бессмысленность корреляции по идентификаторам. |
| 15.08.2026 | Titanic: пропуски, confounding, утечка таргета, EDA, README. **Level 1 закрыт** | Зачёт. Проект сдан: 9 выводов с числами, ограничения, воспроизводимый ноутбук, README. Понял утечку таргета и confounding — не с чужих слов, а найдя оба в своих данных. Сам нашёл, что порт значим и внутри класса, — там, где я ожидал только confounder. Слабое прежнее и главное: шесть раз написал текст, противоречащий выводу ячейки над ним. Анализирует хорошо, переносит в слова плохо. |
| 23.08.2026 | Доверительные интервалы: смысл, покрытие, ширина, где ломаются | Зачёт. Симуляция покрытия дала 95 из 100. Сам заметил, что график противоречит себе (зелёные отрезки не касались линии), и нашёл баг с длиной списка. Главный сдвиг: ширину интервала прочитал верно — в домашке двумя днями раньше читал наоборот. Слабое: три задачи из семи не понял и не переспросил сразу; задачи на ширину ответил по памяти, не запуская; заключительные выводы (вердикт, отношение ширины к оценке) снова не дописаны. |
| 21.08.2026 | Распределения, правило трёх сигм, ЦПТ и стандартная ошибка | Зачёт, сильная сессия. ЦПТ и `SE = σ/√n` вывел из симуляции сам, причём сам же придумал дополнительную проверку на n = 3600. Дважды поймал собственную ошибку до меня — про сторону хвоста. Слабое: выводы делает глазом вместо числа (сказал «логарифм почти не изменил» при падении асимметрии в 12 раз; назвал «узким» интервал от 0 до 26%). Не сравнивает величину с масштабом — смотрит на модуль числа. |
| 18.08.2026 | Статистика: меры центра и разброса, вывод дисперсии, `ddof` | Зачёт. Формулы выводил сам, `ddof` нашёл собственным расследованием — выдвинул гипотезу, проверил, отверг, выдвинул вторую. Слабое прежнее: дважды объяснил механизм метафорой вместо механики (`source`, «две половины компенсировали»), не заметил двойной запуск мутирующей ячейки, запустил симуляцию только для n=30 из двух требовавшихся. |
| 18.08.2026 | Pandas: дриллинг беглости — выборка, агрегация, merge, pivot_table | Зачёт, и лучшая сессия по динамике. Сам попросил вернуться к pandas, диагноз подтвердился цифрами (`loc`/`iloc` по одному разу за всё время, `merge` и `pivot_table` — ноль). Правило «не даю синтаксис» сработало: сам нашёл `filter`, `idxmax`, именованную агрегацию, `validate`, `indicator`. Итог — 12 из 13 задач без подсказок против 40 минут на 10 однострочников на входе. Слабое: пять раз потерял вторую половину условия задачи. |
